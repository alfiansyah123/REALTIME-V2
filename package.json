{
    "name": "realtime-conversion-monitor",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=apps/dashboard",
        "build": "npm run build --workspace=apps/dashboard",
        "preview": "npm run preview --workspace=apps/dashboard"
    },
    "devDependencies": {},
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}